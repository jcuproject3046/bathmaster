<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>

  <!--  
   <package name="oa_default" extends="struts-default">
        <interceptors>
                <interceptor name="loginInterceptor" class="com.esp.web.commom.interceptor.LoginInterceptor"/>
                <interceptor-stack name="os_stack">
            	 	<interceptor-ref name="defaultStack"></interceptor-ref>
                 	<interceptor-ref name="loginInterceptor"></interceptor-ref>
                </interceptor-stack>
                
        </interceptors>
        <default-interceptor-ref name="os_stack"></default-interceptor-ref>
        <global-results>
            <result name="login">/session.jsp</result>
    	</global-results>
    </package>
    -->
    
    <include file="struts/struts-*.xml"></include>
</struts>

